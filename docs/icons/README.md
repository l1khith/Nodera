# Nodera Logo Assets

- `nodera-logo.svg` — primary scalable vector master.
- `nodera-logo-mono.svg` — monochrome `currentColor` variant.

Keep the SVG files as the source assets. Generate ICO/ICNS/PNG derivatives from the SVG during packaging when needed.
